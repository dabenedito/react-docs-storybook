import { Meta, StoryObj } from '@storybook/react';
import { Widget } from './Widget';

export default {
  title: 'Componentes/Widget',
  component: Widget,
} as Meta;

export const Default: StoryObj = {};
